services:
  app:
    image: app
    pull_policy: never
    container_name: app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    env_file:
      - .env
    develop:
      watch:
        - action: sync
          path: src/
          target: /app/src
          ignore:
            - node_modules/
            - .git/
        - action: rebuild
          path: package.json
        - action: restart
          path: .env

  postgres:
    image: postgres:18.2-alpine
    container_name: postgres
    restart: always
    shm_size: 128mb
    ports:
      - 5432:5432
    volumes: 
      - postgres-data:/data/db
    env_file:
      - .env

volumes:
  postgres-data: